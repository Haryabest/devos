# Files Module

Загрузка/скачивание файлов через абстракцию `StorageDriver`:
LOCAL | S3 | MINIO | R2 | B2 — выбор через `STORAGE_DRIVER`.
Multipart upload, стриминг, presigned URL для S3-совместимых.
